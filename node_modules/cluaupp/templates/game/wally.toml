[package]
name = "kartzdev/cluaupp-game"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

# CluauppLibs (copied to ReplicatedStorage.CluauppLibs on init/build) already
# ships Sweep, Spark, Keep, Flare, Mint, Axiom, Roster, Gleam, Bloom, Lens,
# Crest, Pin, Stage, Coil, Helm, Shift, Hive, Ember, Echo, Guide, Trace, Promise, Net.
# Add Wally entries only for packages that are not in CluauppLibs.

[dependencies]
