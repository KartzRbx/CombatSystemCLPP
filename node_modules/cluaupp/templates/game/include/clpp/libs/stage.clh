#pragma once

struct Stage {
	Model Object3D;
	GuiObject AdornFrame;
	Camera Camera;
	bool Visible;
	ViewportFrame Viewport;
	WorldModel World;
	static Stage new_(Instance model);
	static Stage Attach(GuiObject frame, Instance model);
	static Stage Attach3D(GuiObject frame, Instance model);
	void Update();
	void Update(double dt);
	void SetCFrame(CFrame newCF);
	CFrame GetCFrame();
	void SetDepthMultiplier(double multiplier);
	double GetDepthMultiplier();
	void SetActive(bool active);
	bool GetActive();
	void Destroy();
	void End();
};
