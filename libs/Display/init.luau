--!strict
-- Cluaupp Display — typed border over nightcycle/display (_impl).

local Impl = require(script._impl)

export type DisplayConfig = Impl.DisplayConfig
export type Display = Impl.Display
export type DisplayBuilder = Impl.DisplayBuilder

return Impl
